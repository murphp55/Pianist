<?xml version="1.0" encoding="utf-8" ?>
<Application
    x:Class="PianistApp.App"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:converters="clr-namespace:PianistApp.Converters">
    <Application.Resources>
        <ResourceDictionary>
            <Color x:Key="AppBackground">#F3F4F7</Color>
            <Color x:Key="PanelBackground">#FFFFFF</Color>
            <Color x:Key="PanelBorder">#D8D2C8</Color>
            <Color x:Key="Accent">#2D4C7C</Color>
            <Color x:Key="AccentSoft">#E2EAF6</Color>
            <Color x:Key="LeftHand">#8A4E24</Color>
            <Color x:Key="LeftHandSoft">#F3E5DC</Color>
            <Color x:Key="SubtleText">#4E4E4E</Color>
            <Color x:Key="MutedText">#6A6A6A</Color>

            <Style TargetType="ContentPage">
                <Setter Property="BackgroundColor" Value="{StaticResource AppBackground}" />
            </Style>
            <Style TargetType="Label">
                <Setter Property="TextColor" Value="#2E2E2E" />
                <Setter Property="FontSize" Value="14" />
            </Style>
            <Style TargetType="Button">
                <Setter Property="BackgroundColor" Value="{StaticResource Accent}" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="CornerRadius" Value="8" />
                <Setter Property="Padding" Value="12,6" />
            </Style>
            <Style TargetType="Frame">
                <Setter Property="BackgroundColor" Value="{StaticResource PanelBackground}" />
                <Setter Property="BorderColor" Value="{StaticResource PanelBorder}" />
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="HasShadow" Value="False" />
            </Style>

            <converters:BoolToYesNoConverter x:Key="BoolToYesNoConverter" />
        </ResourceDictionary>
    </Application.Resources>
</Application>
